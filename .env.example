# Copy file này → .env (đã gitignored). docker-compose đọc các biến dưới.
# SA password cho SQL Server dev (wh-sqlserver). Đổi nếu cần; phải khớp
# ConnectionStrings:Default trong appsettings.Development.json.
MSSQL_SA_PASSWORD=Workspace#2026Dev
